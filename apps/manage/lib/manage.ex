defmodule Manage do
  @moduledoc false

  def show_telephone_bill(params) do
    {:error, 403, "Something wrong happend"}
  end

  def create_telephone_call(params) do
    {:error, 401, "Oooops"}
  end
end
